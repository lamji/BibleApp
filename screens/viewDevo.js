import * as React from 'react';
import { View, Text, Button } from 'react-native';

function ViewDevo({ navigation }) {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>ViewDevo</Text>
        
      </View>
    );
  }

  export default ViewDevo